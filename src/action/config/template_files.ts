/** This interface represents the Template files to use for the categories. */
export interface TemplateFiles {
  /** Template file for the readme. */
  readme? : string,
  
  /** Template file for the category. */
  category? : string,
  
  /** Template file for the repository. */
  repository? : string
}
