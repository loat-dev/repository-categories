{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Category Config",
  "description": "Configuration for the repository categories GitHub action config.json file.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "title": "Schema",
      "description": "Pointer to the schema against which this document should be validated."
    },
    "templateFiles": {
      "title": "Template Files",
      "description": "Template files to use for the categories.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "readme": {
          "description": "Template file for the readme.",
          "type": "string"
        },
        "category": {
          "description": "Template file for the category.",
          "type": "string"
        },
        "repository": {
          "description": "Template file for the repository.",
          "type": "string"
        }
      }
    },
    "labelSearchPattern": {
      "title": "Label Search Pattern",
      "description": "The pattern to search for in the repository labels.",
      "type": "string"
    },
    "repositoryBlacklist": {
      "title": "Repository Blacklist",
      "description": "List of repository names to blacklist.",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "description": "Repository name to blacklist."
      }
    },
    "categories": {
      "title": "Categories",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "title": "ID",
          "description": "Mappings between the category IDs and the category names.",
          "type": "object",
          "properties": {
            "category_1": {
              "$comment": "This is an example category.",
              "title": "Category Name",
              "description": "The display name of the category, used in the README.",
              "type": "string",
              "default": "Category 1"
            }
          },
          "patternProperties": {
            "": {
              "title": "Category Name",
              "description": "The display name of the category, used in the README.",
              "examples": [ "Category 1" ],
              "type": "string"
            }
          }
        },
        "default": {
          "type": "string",
          "title": "Default",
          "description": "Default category to use if no category is found."
        }
      },
      "required": [ "id", "default" ]
    }
  },
  "required": [
    "templateFiles",
    "categories"
  ]
}
